# AGENTS.md

## 브랜치 정체성

- 현재 브랜치는 `after-mid` 기준 브랜치다.
- 중간보고 이전 구조를 보존하는 것이 목표가 아니다.
- 현재 브랜치는 중간보고 이후 실제 실험 파이프라인을 새로 정리하는 기준선으로 사용한다.

## 연구 방향

- 본 연구는 단순 TCP batching 최적화가 아니다.
- 본 연구는 콘텐츠 중요도와 전송 계층 결정을 결합한 `cross-layer 적응 전송` 연구다.
- 표현은 `cross-layer 적응 전송`, `콘텐츠 중요도 기반 적응 전송`을 사용한다.
- `단순 TCP batching 연구`라는 표현은 사용하지 않는다.

## 절대 규칙

- mock, synthetic, fallback, dummy 데이터 로직을 구현하지 않는다.
- 실제 비디오 파일이 없으면 실험을 진행하지 않는다.
- 실제 네트워크 전송이 불가능하면 축소 실행하지 않고 명시적으로 실패해야 한다.
- trace CSV는 원본 비디오에서 생성한 캐시로만 허용한다.
- 실험 지표는 실제 송신 시각, 실제 수신 시각, 실제 전송 바이트로부터 계산해야 한다.

## 문서 규칙

- 모든 프로젝트 문서는 `docs/` 아래에만 둔다.
- 루트에는 `README.md` 외의 새 문서를 만들지 않는다.
- `docs/initial_plan.md`는 after-mid 시작 시점의 기준 스냅샷으로 유지한다.
- `docs/research_goal.md`는 현재 방향에 맞게 갱신한다.
- `docs/changelog/`와 `docs/analyze/`는 after-mid 이후 생성분부터 append-only로 관리한다.
- `docs/plan/`은 계획 문서를 누적 보관하는 폴더다.
- 앞으로 plan으로 수립된 계획은 반드시 `docs/plan/YYYYMMDD_플랜제목.md` 형식으로 남긴다.
- 과거 브랜치 문서 관행은 현재 브랜치에 승계하지 않는다.

## 코드 구조 규칙

- 응답은 항상 한국어로 작성한다.
- 함수는 짧고 역할이 분명해야 한다.
- 변수명은 축약하지 않고 의미가 드러나야 한다.
- 데이터 전달은 가능하면 `dataclass`를 사용한다.
- 실험 흐름은 아래 순서를 따른다.
  1. 원본 비디오 입력
  2. 프레임 메타데이터 및 전송 단위 준비
  3. Stage A/B 정책 적용
  4. Mininet 실제 전송
  5. 실제 측정값 기반 지표 계산
  6. 노트북 시각화
- 노트북은 오케스트레이션과 분석만 담당한다.
- 정책 판단, 전송 이벤트 기록, 요약 집계는 Python 스크립트가 담당한다.
- `logging`을 사용해 실험 단계와 실패 원인을 추적 가능하게 한다.
- 추가 라이브러리는 반드시 `requirements.txt`에 반영한다.

## 디렉토리 역할

- `core/`: 실제 실험 공통 로직, 비디오 자산 처리, 이벤트 집계
- `policy/`: Stage A/B 정책
- `scripts/`: 실제 실행 가능한 CLI 스크립트
- `dataset/`: 원본 비디오와 원본 기반 캐시
- `output/`: after-mid 실험 산출물

## Git 규칙

- 스테이징은 항상 `git add -A`를 사용한다.
- 커밋 요청을 받으면 별도 동의 없이 `git add -A` 후 한글 메시지로 커밋한다.
- 에이전트 작업 범위는 `git commit`까지이며 `git push`는 수행하지 않는다.
