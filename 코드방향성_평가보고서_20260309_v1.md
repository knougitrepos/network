# 프로젝트 코드 및 방향성 평가 보고서

**작성일**: 2026년 3월 9일
**버전**: v1

## 1. 종합 평가 브리프
현재 `progress_notes.md`에 정의된 문제 상황과 이를 바탕으로 구현된 `tcp-content-aware-batching.ipynb` 시나리오 코드의 **진행 방향성은 매우 훌륭하고 타당합니다.** 
본 모델링을 단순 라벨 분류나 일반적인 DL 예측(CNN/RNN 등)이 아닌 **Policy Learning / Adaptive Control (정책 학습 및 적응형 제어)** 문제로 정의한 것은 현재 다루고자 하는 TCP Buffer / Flush 의사결정 방식과 정확히 일치하며 접근 방법론으로 손색이 없습니다.

## 2. 접근 방식 및 아키텍처 평가

### 2-1. 단계적 해결(Heuristic -> ML -> RL) 로드맵의 타당성 (매우 우수)
베이스라인 정책(Heuristic)으로 시작하여 경량 머신러닝(DT, RF)을 거쳐 최종적으로 부분 관찰 환경에서의 최적 행동 강화를 위한 강화학습(RL)으로 진화하는 파이프라인은 복잡한 시스템 제어 문제를 풀 때 논문에 제시하기 가장 이상적인 형태입니다.
- **Heuristic**: 도메인 지식(RTT, Content-type)을 반영한 하드코딩된 규칙으로 상한선 및 베이스라인 분석.
- **ML (Regression/Tree)**: 최고 성능을 낸 고정 Batch 수치(Oracle)를 라벨로 삼아, 빠른 추론 속도로 준수하고 적응적인 성능 확보.
- **RL**: 이전 큐(Queue) 상태와 RTT 등의 정보를 바탕으로 행동(Flush/Wait) 간의 연속적 피드백 루프를 모델링하여, 지연 시간 최소화와 처리량 최대화라는 복합 보상(Reward)을 극대화 가능.

### 2-2. 논문 작성 관점의 장점과 보완 요소
- **강점**: 
  - `통신 지연(Latency) 최소화` + `처리량(Throughput) 최대화` + `시스템콜(flush) 오버헤드 최소화`는 전통적인 트레이드오프(Trade-off) 목표입니다. 이를 `pareto scatter` 등을 활용해 가시화하려는 접근은 논문 결과물 관측 시 매우 효과적입니다.
  - RL 및 적응형 제어를 왜 써야하는지에 대한 당위성이 잘 잡혀 있습니다.
- **서술/참고문헌 권장**:
  - 논문 Background 및 서론 작성 시, 전통적인 **TCP Nagle 알고리즘과 수신측 Delayed ACK 메커니즘 간의 충돌로 인한 Latency Spike 문제 해결** 연구들을 레퍼런스로 포함시켜 본 시도(Content-Aware Batching)의 목적과 중요성을 부각시키길 권장합니다.

## 3. 코드 구현 평가 (`tcp-content-aware-batching.ipynb`)

현재 작성된 Baseline 골격은 시뮬레이터 역할을 매우 훌륭히 수행하고 있습니다.

### 3-1. 구현 강점 (Strengths)
1. **유연한 설정 및 추상화**: `WorkloadConfig`와 `PolicyConfig`을 훌륭히 분리하고 `dataclass`로 처리하여, 차후 ML을 위한 Feature Engineering 용이나 `fixed_sweep` 스크립트를 작성할 때 구조 변경 없이 확장이 용이합니다.
2. **현실적인 워크로드 분리 모델**: `static_file`(대용량 파일)과 `dynamic_stream`(불규칙한 크기와 주기를 가진 스트림 워크로드)의 특성을 각기 다르게 난수 발생시켜 `generate_workload`를 설계한 구성이 매우 정밀하고 좋습니다.

### 3-2. 유지보수 및 개선 권고 사항 (Improvements & Edge Cases)
1. **결측 및 Edge Case 방어 코드 필요 (핵심)**: 
   - `run_simulation` 실행 중, 매우 짧은 시간이거나 트래픽이 완전히 발생하지 않아 `latencies` 나 `batch_sizes` 배열이 0개가 될 수 있는 Edge Case가 발생할 수 있습니다.
   - 이때 `latency_array.mean()`나 `np.quantile(latency_array, 0.95)` 연산에서 `NaN` 반환 및 에러가 발생할 소지가 있으므로, **배열의 길이가 비어 있을 경우 0.0을 반환하도록 하는 예외 처리 및 방어 코드**를 추가하시면 훨씬 견고한 시뮬레이터가 됩니다.
2. **시뮬레이션 환경의 단순화 (한계점 인식 필요)**: 
   - 현재 시나리오는 대역폭 기반의 수식(`tx_time = payload * 8.0 / bandwidth`)으로 이상적으로 지연성을 계산합니다.
   - 실제 TCP 단에서는 윈도우 사이즈 증감(Slow Start 등) 및 패킷 로스/재전송 등 동적 환경이 존재하므로, 추후 평가 시 한계점(Limitations) 섹션에 "본 평가는 Application 계층 중심의 간소화된 버퍼 플러시 시뮬레이션(Simplified Buffer Flush Simulator)"임을 서술하시는 편이 오해 방지와 방어 논리에 좋습니다.
3. **노트북 셀 주석 관련 규칙 (사용자 룰 반영)**:
   - 프로젝트 공통 규칙인 **노트북 파일의 각 셀 상단에 몇 번 셀인지 목적과 번호를 명시(예: `# cell 1 : 라이브러리 및 환경 설정`)** 하는 부분이 현재 코드에 누락되어 있습니다. 향후 코드 추가 시 리팩토링 차원에서 셀 번호 마킹을 권장합니다.
